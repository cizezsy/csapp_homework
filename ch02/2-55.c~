#include<stdio.h>


void show_bytes(byte* byteptr, int length);

int main() {
  char a[] = "123456789";
  showbytes(a, 9);
  return 0;
}

void show_bytes(byte* byteptr, int length) {
  for(int i = 0; i < length; ++i) {
    printf("%d", *(byteptr+i));
  }
}
